# 需求与 Bug 协作登记表

本文件用于集中登记项目需求和 Bug，方便人类与 AI 协作者在同一个位置了解
待办事项、状态、负责人和相关 GitHub 记录。

本文件长期保留全部条目。需求完成或 Bug 关闭后，不删除原记录，只更新状态、
完成说明和关联 Issue、PR 或提交。

## 1. 使用原则

1. 新增条目前，先在本文件和 GitHub Issues 中搜索是否已有重复记录。
2. 每个需求或 Bug 使用唯一 ID，并同时更新索引和详情。
3. 一个条目只描述一个独立问题；多个无关事项应拆成多个条目。
4. 需求必须包含可验证的验收标准，Bug 必须包含可执行的复现步骤。
5. 未经验证的原因只能写成“可能原因”，不能写成已确认根因。
6. 自动测试通过、生产构建通过和真实浏览器验收是不同证据，必须分别记录。
7. 完成或关闭条目时，补充实际验证结果和关联提交，不覆盖历史结论。
8. 不得写入密钥、token、个人身份信息、私人图片、数据库内容或未脱敏日志。

## 2. ID、状态与优先级

### ID 规则

- 需求：`REQ-YYYYMMDD-NN`
- Bug：`BUG-YYYYMMDD-NN`
- `YYYYMMDD` 为首次登记日期。
- `NN` 从 `01` 开始，同一天同一类型依次递增。

示例：

```text
REQ-YYYYMMDD-NN
BUG-YYYYMMDD-NN
```

创建 ID 前必须在本文件中搜索，确认 ID 没有被占用。

### 需求状态

| 状态 | 含义 |
| --- | --- |
| `待评审` | 已登记，尚未确认是否进入产品范围 |
| `已接受` | 范围与验收标准已确认，等待排期 |
| `进行中` | 已有负责人并开始实施 |
| `已完成` | 验收标准全部满足并已有验证证据 |
| `暂缓` | 当前不实施，但保留后续评估 |
| `已拒绝` | 明确不进入产品范围，并记录原因 |

### Bug 状态

| 状态 | 含义 |
| --- | --- |
| `待复现` | 已报告，但尚未获得稳定复现证据 |
| `已确认` | 已复现并确认属于当前产品问题 |
| `修复中` | 已有负责人并正在修复 |
| `待验证` | 修复已完成，等待独立或目标环境验证 |
| `已关闭` | 修复已验证，或确认无需修复并记录原因 |

### 优先级

| 优先级 | 使用标准 |
| --- | --- |
| `P0` | 数据安全、隐私、不可恢复的数据损失，或核心产品完全不可用 |
| `P1` | 核心流程大面积不可用，且没有合理替代方案 |
| `P2` | 重要功能或体验明显受影响，但存在替代方案或影响范围有限 |
| `P3` | 轻微体验、文案、样式或低频问题，不阻塞主要流程 |

优先级描述影响和紧急程度，不代表实现难度。

## 3. 需求索引

新增需求时，在表格中增加一行，并链接到下方对应 ID 标题。

| ID | 标题 | 优先级 | 状态 | 负责人 | 最后更新 | GitHub |
| --- | --- | --- | --- | --- | --- | --- |
| [REQ-20260728-02](#req-20260728-02) | 增加本地宠物聊天和情绪反馈 | P2 | 已完成 | Codex | 2026-07-28 | 暂无（本地未推送） |
| [REQ-20260728-01](#req-20260728-01) | 增加四向移动、尺寸控制和自适应阴影 | P2 | 已完成 | Codex | 2026-07-28 | 暂无（本地未提交） |

## 4. Bug 索引

新增 Bug 时，在表格中增加一行，并链接到下方对应 ID 标题。

| ID | 标题 | 优先级 | 状态 | 负责人 | 最后更新 | GitHub |
| --- | --- | --- | --- | --- | --- | --- |
| [BUG-20260728-02](#bug-20260728-02) | 玩具球没有位于宠物头部前方 | P2 | 已关闭 | Codex | 2026-07-28 | 暂无（本地未提交） |
| [BUG-20260728-01](#bug-20260728-01) | 食盆与宠物嘴部不对位 | P2 | 已关闭 | Codex | 2026-07-28 | 暂无（本地未提交） |

## 5. 需求详情

新需求按“最新在前”的顺序放在本节。标题只使用 ID，保证索引锚点稳定。

### REQ-20260728-02

- **标题：** 增加本地宠物聊天和情绪反馈
- **状态：** `已完成`
- **优先级：** `P2`
- **提出日期：** 2026-07-28
- **提出人：** 项目维护者
- **负责人：** Codex
- **关联 GitHub：** 暂无（本地未推送）
- **最后更新：** 2026-07-28

#### 用户场景

用户希望像经典宠物游戏一样打开对话框和宠物聊天，并通过颜文字、爱心、
音符、问号等闪烁符号理解宠物当时的情绪。

#### 背景与问题

现有产品具有动作和状态文字，但用户只能点击固定互动，不能输入自己想说的
内容，也没有独立于动作的心情反馈。

#### 目标

提供无需 API 的本地聊天、五种宠物口吻、动作与场景上下文、七种明确心情，
以及对话框内外的像素情绪反馈。

#### 范围

- 包含：像素对话框、单行输入、关键词回复、五宠物口吻、七种心情、
  逐字显示、舞台符号、键盘和减少动态支持。
- 不包含：外部大模型、后端、账号、云端聊天记录、语音和新精灵图。

#### 验收标准

- [x] 对话框可打开和关闭，按钮与 Enter 可发送，空白输入不可发送。
- [x] 七类意图、未匹配输入、当前动作和当前场景产生正确本地回复。
- [x] 五只宠物具有不同口吻，切换后显示新宠物欢迎语。
- [x] 对话框内显示心情和颜文字，宠物周围短暂显示跟随符号。
- [x] 输入方向键不移动宠物，Escape、焦点和完整回复播报可用。
- [x] 聊天保留场景、二维位置、尺寸及既有互动行为。
- [x] 减少动态模式直接显示完整回复并停止闪烁。
- [x] 自动测试、类型检查和生产构建通过。
- [x] `1440×900`、`578×863`、`390×844` 完成真实浏览器验收。

#### 影响范围

- 可能涉及的页面、模块或文件：聊天模型、互动组件、共享样式、测试和文档。
- 依赖：现有宠物、场景目录和动作状态机。
- 风险：移动端对话框可能遮挡宠物，输入方向键可能错误触发舞台移动。

#### 状态记录

| 日期 | 状态变化 | 说明与证据 |
| --- | --- | --- |
| 2026-07-28 | 新建为 `进行中` | 采用完全本地的关键词聊天方案 |
| 2026-07-28 | `进行中` → `已完成` | 自动测试、构建与内置浏览器验收通过；证据见 `docs/qa/pixel-pet-chat-emotions-qa.json` |

### REQ-20260728-01

- **标题：** 增加四向移动、尺寸控制和自适应阴影
- **状态：** `已完成`
- **优先级：** `P2`
- **提出日期：** 2026-07-28
- **提出人：** 项目维护者
- **负责人：** Codex
- **关联 GitHub：** 暂无（本地未提交）
- **最后更新：** 2026-07-28

#### 用户场景

用户希望宠物不只左右移动，还能在场景中向前、向后移动，并能用滑块
调整宠物大小。位置和大小变化时，阴影需要同步变化，互动动作需要放慢。

#### 背景与问题

旧版只保存一个横向位置，阴影使用固定尺寸，一次性动作约
`700–1030ms`，较难看清完整过程。

#### 目标

宠物具有二维舞台位置、`70%–125%` 尺寸滑块、景深缩放和自适应阴影；
一次性互动动作比旧版慢约 `45%`。

#### 范围

- 包含：四向按钮和方向键、二维边界、尺寸滑块、景深、阴影和动作速度。
- 不包含：重新生成正面或背面行走素材、3D、体素、WebGL。

#### 验收标准

- [x] 指针和四个方向键均可移动，`x = 8–92`、`y = 20–80` 不越界。
- [x] 尺寸滑块范围为 `70%–125%`，默认 `100%`。
- [x] 宠物、阴影和互动道具随用户尺寸和景深同步变化。
- [x] 一次性动作帧时长增加约 `45%`，移动与循环动作节奏不变。
- [x] 自动测试、类型检查和生产构建通过。
- [x] 已在 `1440×900`、`578×863`、`390×844` 真实浏览器检查。

#### 影响范围

- 可能涉及的页面、模块或文件：互动宠物模型、目录、组件、样式、测试和产品文档。
- 依赖：现有九行动画图集。
- 风险：现有图集没有正面和背面行走帧，前后移动只能采用已确认的 2.5D 表现。

#### 状态记录

| 日期 | 状态变化 | 说明与证据 |
| --- | --- | --- |
| 2026-07-28 | 新建并完成为 `已完成` | `docs/qa/pixel-pet-depth-movement-qa.json` 记录自动和浏览器验收 |

## 6. Bug 详情

新 Bug 按“最新在前”的顺序放在本节。标题只使用 ID，保证索引锚点稳定。

### BUG-20260728-02

- **标题：** 玩具球没有位于宠物头部前方
- **状态：** `已关闭`
- **优先级：** `P2`
- **报告日期：** 2026-07-28
- **报告人：** 项目维护者
- **负责人：** Codex
- **发生频率：** 必现
- **关联 GitHub：** 暂无（本地未提交）
- **最后更新：** 2026-07-28

#### 环境

- Commit：基于 `9988b39` 的本地未提交工作区
- 分支：`codex/interactive-pixel-dog`
- 操作系统：macOS
- 浏览器与版本：Codex 内置浏览器
- 设备或视口：桌面和移动端
- 其他必要配置：无

#### 问题摘要

旧版玩具球使用场景级固定横向偏移，在真实图集中会落到宠物尾部附近，
而不是头部前方。

#### 前置条件

选择任意宠物。

#### 复现步骤

1. 打开互动宠物首页。
2. 点击“玩球”。
3. 观察球与宠物头部的相对位置。

#### 期望结果

球位于头部朝向的前方；接近左边界时角色与球同步镜像并保持在场景内。

#### 实际结果

球与头部使用不同的固定定位逻辑，柴犬上会显示在尾部附近。

#### 用户影响

玩球动作语义错误，用户难以理解宠物正在和球互动。

#### 证据

- 截图或视频：本轮真实浏览器逐宠物检查。
- 已脱敏日志：无。
- 控制台错误：无。
- 自动测试或最小复现：`PixelDogStudio.test.tsx` 的头部前方和边界镜像测试。

#### 临时解决方法

暂无。

#### 可能涉及范围

已确认涉及玩球图集原始朝向、宠物局部锚点和边界镜像规则。

#### 修复验收标准

- [x] 原始步骤不再复现问题。
- [x] 已加入或更新回归测试。
- [x] 相关测试、类型检查和构建通过。
- [x] 五只宠物和目标视口完成真实浏览器复验。
- [x] 没有新增控制台错误或相关回归。

#### 状态记录

| 日期 | 状态变化 | 说明与证据 |
| --- | --- | --- |
| 2026-07-28 | 新建并关闭为 `已关闭` | 五只宠物的球均在头部前方；左边界同步镜像 |

### BUG-20260728-01

- **标题：** 食盆与宠物嘴部不对位
- **状态：** `已关闭`
- **优先级：** `P2`
- **报告日期：** 2026-07-28
- **报告人：** 项目维护者
- **负责人：** Codex
- **发生频率：** 必现
- **关联 GitHub：** 暂无（本地未提交）
- **最后更新：** 2026-07-28

#### 环境

- Commit：基于 `9988b39` 的本地未提交工作区
- 分支：`codex/interactive-pixel-dog`
- 操作系统：macOS
- 浏览器与版本：Codex 内置浏览器
- 设备或视口：桌面和移动端
- 其他必要配置：无

#### 问题摘要

旧版食盆根据宠物横向位置放在身体左侧或右侧，没有与进食帧中的嘴部对位。

#### 前置条件

选择任意宠物。

#### 复现步骤

1. 打开互动宠物首页。
2. 点击“喂食”。
3. 观察食盆与宠物嘴部的位置。

#### 期望结果

食盆位于进食动作中嘴部的下方，并随宠物位置和尺寸移动。

#### 实际结果

食盆出现在宠物身体外侧，嘴部没有朝向食盆。

#### 用户影响

进食动作缺少空间关联，明显削弱互动可信度。

#### 证据

- 截图或视频：本轮真实浏览器逐宠物检查。
- 已脱敏日志：无。
- 控制台错误：无。
- 自动测试或最小复现：`PixelDogStudio.test.tsx` 的嘴部锚点测试。

#### 临时解决方法

暂无。

#### 可能涉及范围

已确认涉及旧的场景级食盆偏移和缺少宠物局部嘴部锚点。

#### 修复验收标准

- [x] 原始步骤不再复现问题。
- [x] 已加入或更新回归测试。
- [x] 相关测试、类型检查和构建通过。
- [x] 五只宠物和目标视口完成真实浏览器复验。
- [x] 没有新增控制台错误或相关回归。

#### 状态记录

| 日期 | 状态变化 | 说明与证据 |
| --- | --- | --- |
| 2026-07-28 | 新建并关闭为 `已关闭` | 五只宠物的食盆均在嘴部下方并跟随共享舞台锚点 |

## 7. 可复制模板

新增真实条目时，将对应模板复制到详情区，并删除模板中的说明文字。不要直接
修改本节模板来登记事项。

### 需求模板

```markdown
### REQ-YYYYMMDD-NN

- **标题：**
- **状态：** `待评审`
- **优先级：** `P2`
- **提出日期：** YYYY-MM-DD
- **提出人：**
- **负责人：** 未分配
- **关联 GitHub：** Issue / PR / Commit；没有时写“暂无”
- **最后更新：** YYYY-MM-DD

#### 用户场景

说明谁在什么情况下遇到什么需求。

#### 背景与问题

描述当前情况，以及为什么需要改变。

#### 目标

说明完成后用户可以观察到的结果。

#### 范围

- 包含：
- 不包含：

#### 验收标准

- [ ] 条件一
- [ ] 条件二
- [ ] 已说明需要执行的自动测试
- [ ] 如涉及视觉，已说明目标视口和真实浏览器检查

#### 影响范围

- 可能涉及的页面、模块或文件：
- 依赖：
- 风险：

#### 状态记录

| 日期 | 状态变化 | 说明与证据 |
| --- | --- | --- |
| YYYY-MM-DD | 新建为 `待评审` | 初始登记 |
```

需求索引示例：

```markdown
| [REQ-YYYYMMDD-NN](#req-yyyymmdd-nn) | 增加示例功能 | P2 | 待评审 | 未分配 | YYYY-MM-DD | 暂无 |
```

### Bug 模板

```markdown
### BUG-YYYYMMDD-NN

- **标题：**
- **状态：** `待复现`
- **优先级：** `P2`
- **报告日期：** YYYY-MM-DD
- **报告人：**
- **负责人：** 未分配
- **发生频率：** 必现 / 高频 / 偶发 / 仅一次
- **关联 GitHub：** Issue / PR / Commit；没有时写“暂无”
- **最后更新：** YYYY-MM-DD

#### 环境

- Commit：
- 分支：
- 操作系统：
- 浏览器与版本：
- 设备或视口：
- 其他必要配置：

#### 问题摘要

用一句话说明哪里出现了什么错误。

#### 前置条件

列出复现前必须满足的状态；没有时写“无”。

#### 复现步骤

1. （填写第一步）
2. （填写第二步）
3. （填写第三步）

#### 期望结果

说明正确行为。

#### 实际结果

说明实际观察到的行为，不在这里猜测根因。

#### 用户影响

说明影响对象、范围、是否阻塞主要流程及是否有替代方案。

#### 证据

- 截图或视频：
- 已脱敏日志：
- 控制台错误：
- 自动测试或最小复现：

不得提交密钥、个人信息、私人图片或未脱敏数据。

#### 临时解决方法

没有时写“暂无”。

#### 可能涉及范围

记录初步定位。未正式验证前使用“可能”，不要写成确定根因。

#### 修复验收标准

- [ ] 原始步骤不再复现问题
- [ ] 已加入或更新回归测试
- [ ] 相关测试、类型检查和构建通过
- [ ] 如涉及视觉，已在目标视口完成真实浏览器复验
- [ ] 没有引入新的控制台错误或相关回归

#### 状态记录

| 日期 | 状态变化 | 说明与证据 |
| --- | --- | --- |
| YYYY-MM-DD | 新建为 `待复现` | 初始报告 |
```

Bug 索引示例：

```markdown
| [BUG-YYYYMMDD-NN](#bug-yyyymmdd-nn) | 示例页面按钮失效 | P2 | 待复现 | 未分配 | YYYY-MM-DD | 暂无 |
```

## 8. GitHub 协作流程

### 登记新事项

1. 拉取最新代码并搜索重复条目。
2. 创建唯一 ID。
3. 在对应索引中增加一行。
4. 将模板复制到对应详情区并完整填写。
5. 一个分支和提交尽量只登记一个独立事项。
6. 推送分支并创建 Pull Request，邀请协作者评审。

推荐分支名：

```text
docs/req-YYYYMMDD-short-title
docs/bug-YYYYMMDD-short-title
```

推荐提交信息：

```text
docs: add REQ-YYYYMMDD-NN
docs: add BUG-YYYYMMDD-NN
```

示例命令：

```bash
git switch -c docs/req-YYYYMMDD-short-title
git add REQUIREMENTS_AND_BUGS.md
git commit -m "docs: add REQ-YYYYMMDD-NN"
git push -u origin docs/req-YYYYMMDD-short-title
```

除非项目维护者明确允许，不要直接向 `main` 推送协作登记。

### 更新或关闭事项

1. 更新索引中的状态、负责人、最后更新日期和 GitHub 链接。
2. 更新详情中的状态字段。
3. 在“状态记录”中追加一行，不覆盖以前的状态变化。
4. `已完成` 或 `已关闭` 必须附带 PR、提交或可复核验证证据。
5. 不删除已完成、已关闭、暂缓或已拒绝的条目。

## 9. 提交前检查

- [ ] ID 唯一，类型和日期正确。
- [ ] 索引与详情同时存在，锚点链接正确。
- [ ] 标题、状态、优先级、日期和负责人已填写。
- [ ] 需求包含可验证验收标准。
- [ ] Bug 包含环境、复现步骤、期望结果和实际结果。
- [ ] 事实、推测和已确认根因已经明确区分。
- [ ] 没有密钥、隐私信息或未脱敏数据。
- [ ] GitHub Issue、PR 或提交链接准确；没有时明确写“暂无”。
- [ ] `git diff --check` 通过。
